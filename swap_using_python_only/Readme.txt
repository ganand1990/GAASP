First run inputs.py. It will create the input files.
Second run child.py. It will run lammps and do the necessary swapping(Only does one swap for now)
After completing 2 text files containing the energies and 2 data file will be there